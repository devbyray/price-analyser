# Server Configuration
PORT=3000

# Scheduler Configuration
# Cron expression for daily price checks (default: midnight)
CRON_SCHEDULE=0 0 * * *

# Database Configuration
# The database will be stored in the data directory by default
# No need to configure anything here unless you want to use a different location

# Logging Configuration
LOG_LEVEL=info
